<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jitenderkumar.demoducat">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.   -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="whateverString"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:appComponentFactory">
        <!--<receiver
            android:name=".broadcast.MyReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.journaldev.AN_INTENT" />
            </intent-filter>
        </receiver>-->
<!--

        <service android:name=".services.MainService" />
-->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".imagevideo.ImageVideoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SecondActivity" />

        <!-- //<activity android:name=".activities.LoginActivity"></activity> -->
        <!-- <activity android:name=".activities.MainFrgamentActivity"></activity> -->
        <!-- <activity android:name=".activities.recyclerviewdemo.MainRecyclerActivity"></activity> -->
        <!-- <activity android:name=".asynctask.AsyncActivity"></activity> -->
        <!-- <activity android:name=".events.EventActivity"></activity> -->
        <!--
            <activity
            android:name=".menus.NavigationDrawerActivity"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/AppTheme"></activity>
        -->
        <!-- <activity android:name=".dialogs.DialogActivity"></activity> -->
        <!-- <activity android:name=".webview.WebViewActvity"></activity> -->
        <!-- <activity @android:name=".webview.SpinnerDemoActivity"></activity> -->
        <!-- <activity android:name=".broadcast.MainBroadCastActivity"></activity> -->
        <!-- <activity android:name=".broadcast.LocalBroadCastActvity"></activity> -->
        <!-- <activity android:name=".services.MainServiceActivity"></activity> -->
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <!--
               <activity
            android:name=".map.MapsActivity"></activity>
        -->
<!--
        <activity android:name=".imagevideo.ImageVideoActivity"></activity>
-->
    </application>

</manifest>